// // Graphical variables
// @import "config/fonts";
// @import "config/colors";
// @import "config/bootstrap_variables";

// // Bootstrap - DEVE VIR ANTES dos componentes que usam suas variáveis!
// @import "bootstrap";

// // Your CSS partials
// @import "components/index";
// @import "pages/index";

// Seus estilos customizados// Seus estilos customizados (sem imports do Bootstrap)
.list-card {
  transition: all 0.3s ease;

  &:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 30px rgba(0,0,0,0.15) !important;
  }

  .card-img-top {
    transition: transform 0.5s ease;
  }

  &:hover .card-img-top {
    transform: scale(1.05);
  }
}

.bg-gradient {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.list-placeholder {
  height: 200px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.card {
  animation: fadeIn 0.5s ease;
}
